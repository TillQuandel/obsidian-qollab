# Was NICHT in den Wissensgraphen gehoert.
#
# Grund: Generierte Artefakte und Messdaten wandern sonst als eigene Knoten in
# den Graphen und verrauschen die Community-Namen — dasselbe Muster, das bei
# jobfinder 66 gespeicherte API-Antworten und 33 CSVs in die Architektur-Labels
# gezogen hat (Vault-Note [[graphify]], Praxis-Test 2026-08-02).

# Abhaengigkeiten und Baustaende
node_modules/
.worktrees/
graphify-out/
package-lock.json

# Gebaute Bundles — main.js im Wurzelverzeichnis ist der Deploy-Artefakt,
# real*.cjs sind die gebuendelten Messstaende des Spike-Apparats.
main.js
**/real*.cjs
**/*.cjs

# ALTE CODE-KOPIE (ergaenzt 2026-08-13). `tests/_v040/` haelt den v0.4.0-Stand
# von main.ts, sync-handler.ts und text-merge.ts als Vergleichsfassung fuer
# Regressionstests. Im Graphen waeren das Doppelgaenger der echten Module — jede
# God-Node-Zaehlung und jede Community-Zuordnung waere dadurch verfaelscht.
obsidian-crdt-sync/tests/_v040/

# Messergebnisse des Spike-Apparats: reine Zahlenkolonnen, kein Wissen ueber
# die Architektur. Die Instrumente selbst (*.mjs, *.ts) bleiben drin — sie
# tragen die Begruendung im Kopfkommentar.
#
# Breiter gefasst am 2026-08-13: Der Erstlauf gegen dieses Repo zog 48 .txt aus
# spike/ in den Graphen, von denen die beiden alten Muster nur einen Teil
# trafen. Die Zahlenkolonnen heissen dort auch `bilanz-*.txt`, `schnitt-*.txt`
# und aehnlich.
obsidian-crdt-sync/spike/**/*.txt
obsidian-crdt-sync/spike/**/*.log

# Sessionberichte und Messhistorie: lange deutsche Prosa, die sich sonst in die
# Code-Struktur mischt. Der Zweck des Projekts steht in docs/produktziel.md und
# CLAUDE.md — die bleiben ausdruecklich drin.
.superpowers/

# Laufzeit-Muell
debug.log
*.zip
